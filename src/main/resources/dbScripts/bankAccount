-- Create table
create table BANK_ACCOUNT
(
  ID        BIGINT not null,
  FULL_NAME VARCHAR(128) not null,
  BALANCE   DOUBLE not null
) ;

alter table BANK_ACCOUNT   add constraint BANK_ACCOUNT_PK primary key (ID);
 
 
Insert into Bank_Account(ID, Full_Name, Balance) values (1, 'Sathish', 500);
Insert into Bank_Account(ID, Full_Name, Balance) values (2, 'Kariyanna', 600);
Insert into Bank_Account(ID, Full_Name, Balance) values (3, 'King', 700);
Insert into Bank_Account(ID, Full_Name, Balance) values (4, 'Mike', 800);
commit;